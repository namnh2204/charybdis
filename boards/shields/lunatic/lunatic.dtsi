#include <dt-bindings/zmk/matrix_transform.h>
#include "lunatic-layouts.dtsi"

/ {
    chosen {
    // zmk,kscan = &kscan_composite;    //for merge matrix onl
    zmk,kscan = &kscan0; 
    zmk,physical-layout = &physical_layout;
    zmk,battery = &vbatt;
    zephyr,console = &cdc_acm_uart;
    };

	default_transform: keymap_transform_0 {
	        compatible = "zmk,matrix-transform";
	        columns = <12>;
	        rows = <4>;
	                   map = <
				RC(0,0)	RC(0,1)	RC(0,2)	RC(0,3)	RC(0,4)	RC(0,5)		RC(0,6)	RC(0,7)	RC(0,8)	RC(0,9)	RC(0,10) RC(0,11)
				RC(1,0)	RC(1,1)	RC(1,2)	RC(1,3)	RC(1,4)	RC(1,5)		RC(1,6)	RC(1,7)	RC(1,8)	RC(1,9)	RC(1,10) RC(1,11)
				RC(2,0)	RC(2,1)	RC(2,2)	RC(2,3)	RC(2,4)	RC(2,5)		RC(2,6)	RC(2,7)	RC(2,8)	RC(2,9)	RC(2,10) RC(2,11)
										RC(3,3)	RC(3,4)	RC(3,5)		RC(3,6)	RC(3,7)

	        >;
    };



   kscan0: kscan_matrix {
        compatible = "zmk,kscan-gpio-matrix";        // define matrix here...
	        diode-direction = "col2row";
			wakeup-source;


    };

	//encoder_left: encoder_left {
    //    compatible = "alps,ec11";
    //  		a-gpios = <&gpio1 2 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
	//   	    b-gpios = <&gpio1 7 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
	//	steps = <24>;
    //  		status = "disabled";
	//};


	//encoder_right: encoder_right {
	//	compatible = "alps,ec11";
    //  		a-gpios = <&gpio1 2 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
	//   	    b-gpios = <&gpio1 7 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
	//	steps = <24>;
    //   		status = "disabled";
	//};

    //	sensors: sensors {
	//        compatible = "zmk,keymap-sensors";
	//        sensors = <&encoder_left &encoder_right>;
	//        triggers-per-rotation = <12>;
	//};  

	 vbatt: vbatt {
	        compatible = "zmk,battery-nrf-vddh";
	       init-delay-ms = <50>;
	    };

};

// Allow USB logging uncomment to use it
 &usbd {
	status = "okay";
	cdc_acm_uart: cdc_acm_uart {
		compatible = "zephyr,cdc-acm-uart";
		label = "CDC_ACM_0";
	};
};

 &physical_layout {
    transform = <&default_transform>;
    // kscan = <&kscan_composite>; //
    };

&adc {
    status = "okay";
};
&gpiote {
    status = "okay";
};

&gpio0 {
    status = "okay";
};

&gpio1 {
    status = "okay";
};



